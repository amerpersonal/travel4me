App for traveling. Play 2.5.3 version is used for this app. App uses all futures of Scala reactive programming.

TODO:

- use flickrimages that are biggest for 1st picture which is shown as thumnnail
- test whole app
- check dependencies and remove unnecessary
- implement auth tokens API so app can be transfered completely to JSON API instead of using MVC
- consider deployement options for app
- using json4s for deserializing json bodies in case classes
